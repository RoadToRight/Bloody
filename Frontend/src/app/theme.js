import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
            main: "#E51E26",
            dark:"#212121"
        },
        text: {
            primary: "#fff"
        },

    },
});

export default theme;